name: TOTO Results Scraper

on:
  schedule:
    # Monday at 10 PM SGT (2 PM UTC)
    - cron: '0 14 * * 1'
    # Thursday at 10 PM SGT (2 PM UTC)
    - cron: '0 14 * * 4'
    # Friday at 10 PM SGT (2 PM UTC)
    - cron: '0 14 * * 5'

  # Manual trigger from GitHub
  workflow_dispatch:

jobs:
  scrape-toto-results:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install requests beautifulsoup4 python-dotenv pytz

    - name: Run TOTO scraper
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        echo "Running TOTO results scraper..."
        python web-scraper.py